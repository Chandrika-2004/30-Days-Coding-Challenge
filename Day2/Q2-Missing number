Q ; Given an array nums containing n distinct numbers in the range [0, n], return the only number in the range that is missing from the array.

Sol 1:

Python :
class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        n = len(nums)
        for i in range(n+1):
            if i not in nums:
                return i

Sol 2:

class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        n = len(nums)
        cursum = sum(nums)
        actsum = (n*(n+1))/2
        return int(actsum-cursum)
